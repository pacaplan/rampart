default: &default
  adapter: postgresql
  encoding: unicode
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

# Test database - separate from dev to avoid data conflicts
test:
  <<: *default
  host: 127.0.0.1
  port: 54322
  username: postgres
  password: postgres
  database: cat_content_test
  schema_search_path: cat_content

