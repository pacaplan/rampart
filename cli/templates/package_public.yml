# Public API for this bounded context
# Only classes here are accessible to other engines
enforce_dependencies: true
enforce_privacy: false
dependencies:
  - "engines/{{CONTEXT_NAME}}/app/application/{{CONTEXT_NAME}}"
